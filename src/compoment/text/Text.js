import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Text = ({title}) => {
  return (
    <View>
      <Text>{title}</Text>
    </View>
  )
}

export default Text

const styles = StyleSheet.create({})